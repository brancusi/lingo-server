source 'https://rubygems.org'

gem 'rails', '4.2.3'
gem 'pg'
gem 'jbuilder', '~> 2.0'

# webrtc gems
gem "opentok", "~> 2.2"

# Cors for JSON api
gem 'rack-cors', :require => 'rack/cors'

group :development, :test do
  gem 'sqlite3'
  gem 'dotenv-rails'
  gem 'byebug'
  gem 'pry'
  gem 'web-console', '~> 2.0'
  gem 'spring'
end
